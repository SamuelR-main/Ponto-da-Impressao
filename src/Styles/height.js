{/*import { createContext, useContext } from "react";
import { valueHeight } from "../Pages/Homepage/styles-homepage";

const HeightContext = createContext();

export const HeightLocal = ({ Backgroundd }) => {
    const HeightW = {valueHeight}
    return(
    <HeightContext.Provider value={HeightW}>
        {Backgroundd}
    </HeightContext.Provider>
    );
};

export const useHeightContext = useContext(HeightContext);

export function changerValue(px){
    const heightValueOriginalll = '';
    return(heightValueOriginal == px)
};
export const heightValueOriginal = this.heightValueOriginalll
*/}